# processor_result.py (lightweight)

## Summary
`ProcessorResult` dataclass with `success: bool` and `data: Union[str, pd.Series, Dict]`. Used as the universal return type for all processors.

## Branches
None (pure dataclass, 0 branches).
