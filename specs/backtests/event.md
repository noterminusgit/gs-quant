# event.py (lightweight)

## Summary
Event classes for backtest engine: Event (base), MarketEvent, ValuationEvent, OrderEvent, FillEvent. Each stores a type string and relevant data.

## Branches
None (pure data classes, 0 branches).
