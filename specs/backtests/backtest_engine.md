# backtest_engine.py (lightweight)

## Summary
Abstract base class `BacktestBaseEngine` with single abstract method `get_action_handler(action) -> TActionHandler`.

## Branches
None (pure abstract, 0 branches).
