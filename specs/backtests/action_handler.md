# action_handler.py (lightweight)

## Summary
ActionHandler base class with `action` property and abstract `apply_action`. ActionHandlerBaseFactory with abstract `get_action_handler`. TActionHandler TypeVar.

## Branches
None (pure abstract, 0 branches).
